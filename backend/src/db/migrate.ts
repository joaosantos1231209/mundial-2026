// Com PostgreSQL/Supabase, usa `npm run db:push` para sincronizar o schema.
// Este ficheiro existia para migrações SQLite manuais e já não é necessário.
console.log('Usa `npm run db:push` para aplicar o schema no Supabase.');
process.exit(0);
